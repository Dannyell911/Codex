# Codex Minimal Python Project

Use this tiny repo to test Codex **Ask** and **Code**.

Try asking Codex:
- "Опиши архитектуру проекта"
- "Добавь функцию `double(n)` в `app.py` и используй её в `__main__`"
